# 热门内容推荐平台 - 需求文档

## 1. 需求概述

- **项目名称**：热门内容推荐平台（MediaHub）
- **核心目标**：聚合短剧、小说、漫画、动漫的热门内容，提供搜索、推荐、追踪的一站式体验
- **目标用户**：内容消费者（追剧、追漫、追小说的用户）
- **核心价值**：帮助用户发现热门内容、管理观漫记录、获得个性化推荐

---

## 2. 功能需求

### P0（核心功能）

| 功能点 | 描述 |
|--------|------|
| **内容分类展示** | 首页按类型（短剧/小说/漫画/动漫）展示热门内容列表，支持 Tab 切换 |
| **详情页** | 每条内容有独立详情页，展示基本信息（名称、封面、简介、演员/作者/更新状态等） |
| **搜索功能** | 支持按「主演/角色/IP名称」关键词搜索，实时返回结果 |
| **标记已看** | 用户可对内容/章节标记「已看」状态 |
| **推荐系统** | 基于已看内容，跨分类推荐相似IP、同演员、同角色的热门内容 |

### P1（重要功能）

| 功能点 | 描述 |
|--------|------|
| **关联推荐** | 详情页展示「相似内容」「同一IP的其它形式」（如动漫→漫画→小说） |
| **分类筛选** | 支持按年代、地区、热度排序筛选 |
| **观看历史** | 用户个人中心查看已标记的内容列表 |

### P2（增强功能）

| 功能点 | 描述 |
|--------|------|
| **用户体系** | 登录/注册，保存观看偏好 |
| **收藏功能** | 收藏感兴趣的内容 |
| **内容更新提醒** | 收藏的内容更新时提示（可选） |

---

## 3. 非功能需求

| 维度 | 要求 |
|------|------|
| **性能** | 搜索结果 300ms 内返回；首页加载 < 1s |
| **可用性** | 支持主流浏览器（Chrome/Firefox/Safari/Edge）；移动端适配 |
| **数据规模** | 单品类支持 1000+ 条内容数据 |
| **前端框架** | React / Vue（待定） |
| **后端框架** | Node.js + Express / NestJS（待定） |
| **数据库** | PostgreSQL / MongoDB（待定） |

---

## 4. 边界与约束

- **不做什么**
  - 不提供视频/小说/漫画的在线播放/阅读
  - 不接入第三方支付
  - 不做UGC内容生产

- **技术限制**
  - 数据为模拟/种子数据，非真实API对接
  - 推荐算法为基于规则的简单推荐（非ML）

- **外部依赖**
  - 无需第三方登录（可选）
  - 数据源为后端手动生成的模拟数据

---

## 5. 验收标准

### 5.1 首页与内容展示

```
Given 用户打开首页
When 页面加载完成
Then 展示「短剧」「小说」「漫画」「动漫」四个Tab
And  每个Tab下展示该分类的热门内容列表（封面+标题+简介）
And  点击Tab可切换分类
```

### 5.2 搜索功能

```
Given 用户在搜索框输入关键词（如演员名"张三"）
When  点击搜索或按下回车
Then  返回包含该关键词的内容列表
And  结果按热度排序
And  支持模糊匹配（如输入"张三"能匹配到"张三丰"）
```

### 5.3 详情页

```
Given 用户点击任意内容卡片
When  跳转至详情页
Then  展示完整信息：封面、标题、演员/作者/声优、简介、标签
And  展示「关联推荐」区块（同IP其它形式、相似内容）
And  展示「标记已看」按钮
```

### 5.4 标记已看与推荐

```
Given 用户在详情页点击「标记已看」
When  操作成功
Then  该内容状态变为「已看」
And  系统基于已看记录生成「为你推荐」列表
And  推荐结果可在个人中心查看
```

### 5.5 关联推荐

```
Given 用户在详情页查看关联推荐
When  页面加载
Then  展示同一IP的其它形式内容（如《斗破苍穹》动漫→展示《斗破苍穹》漫画/小说）
And  展示相似推荐（同演员/同角色/同类型）
```

### 5.6 后端数据生成

```
Given 后端服务启动
When  系统初始化
Then  自动生成模拟数据：各分类 100 条内容
And  每条内容包含：id、title、cover、summary、actors/author、tags、type、hotScore
And  数据支持增删改查
```

---

## 6. 假设与待澄清

| 序号 | 问题 | 选项 |
|------|------|------|
| 1 | 前端技术选型？ | A. React  B. Vue  C. 无偏好 |
| 2 | 后端技术选型？ | A. Node.js + Express  B. NestJS  C. 无偏好 |
| 3 | 数据库选型？ | A. PostgreSQL  B. MongoDB  C. SQLite（轻量） |
| 4 | 是否需要登录功能？ | A. 需要  B. 暂不需要（本地存储） |
| 5 | 推荐逻辑细化？ | A. 同一IP跨形式  B. 同演员/角色  C. 热度加权  D. 混合 |

---

**确认后可用「方案设计」进入下一阶段，或先澄清上述待定问题。**
